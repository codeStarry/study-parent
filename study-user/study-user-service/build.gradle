plugins {
    id 'java'
    //引入springBoot支持
    id 'io.spring.dependency-management'
}

dependencies {
    compile project(":study-user:study-user-domain")
}

jar {
    enabled(true)
    baseName('study-user-service')
    version('1.0.0-SNAPSHOT')
}
