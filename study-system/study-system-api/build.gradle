plugins {
    id 'java'
    //引入springBoot支持
    id 'io.spring.dependency-management'
}

dependencies {
    compile project(":study-system:study-system-service")
}

jar {
    enabled(true)
    baseName('study-system-api')
    version('1.0.0-SNAPSHOT')
}
