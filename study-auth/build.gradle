plugins {
    id 'java'
    //引入springBoot支持
    id 'io.spring.dependency-management'
}

dependencies {
    compile project(":study-tools")
}

jar {
    enabled(true)
    baseName('study-auth')
    version('1.0.0-SNAPSHOT')
}
